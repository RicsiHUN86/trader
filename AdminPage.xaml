<Page x:Class="trader.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:trader"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AdminPage">

    <Grid Margin="50">

        <Grid.RowDefinitions>
            <RowDefinition Height="31*"/>
            <RowDefinition Height="144*"/>
        </Grid.RowDefinitions>
        <TextBox x:Name="idTextBox" Margin="70,21,547,10">ID</TextBox>
        <Button x:Name="deleteUserButton" Content="törlése" Width="120" Height="30" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="510,18,0,0" Click="deleteUserButton_Click"/>
        <DataGrid
            x:Name="userDataGrid" Margin="70,48,70,72" Grid.RowSpan="2"
            />
    </Grid>
</Page>
